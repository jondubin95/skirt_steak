# Copilot Instructions (Legacy Root Shim)

This root file is kept for compatibility with tools that still look for `copilot-instructions.md` at repo root.

Canonical instructions live in:

- `.github/copilot-instructions.md`

When both files are present, treat `.github/copilot-instructions.md` as the source of truth.
