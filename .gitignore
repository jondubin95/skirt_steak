__pycache__/
*.pyc
*.pyo

# Google Drive credentials & tokens
credentials.json
.google_drive_token.pickle
venv/
.venv/
env/
.DS_Store

# Local sensitive files. Track only the scaffold README.
/Sensitive/**
!/Sensitive/README.md

# Public handoff tier is tracked on purpose (sanitized thread heads only).
# Full-fidelity notes live in Sensitive/handoffs/ and are covered by the
# /Sensitive/** rule above.

# Nested local repo
python/Project/Gem_Factory/gem-factory/
